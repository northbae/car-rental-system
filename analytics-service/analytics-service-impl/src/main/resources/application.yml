server:
  port: 8120

spring:
  application:
    name: analytics-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
      show-components: always
  health:
    kafka:
      enabled: false
    liquibase:
      enabled: false
